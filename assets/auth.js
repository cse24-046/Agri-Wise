document.addEventListener("DOMContentLoaded",()=>{const signup=document.getElementById("signupForm"),login=document.getElementById("loginForm");document.querySelectorAll("[data-auth]").forEach(button=>button.addEventListener("click",()=>{const isSignup=button.dataset.auth==="signup";signup.classList.toggle("hidden",!isSignup);login.classList.toggle("hidden",isSignup);document.querySelectorAll("[data-auth]").forEach(tab=>tab.classList.toggle("active",tab===button))}));signup.addEventListener("submit",event=>{event.preventDefault();APP.set("user",{name:document.getElementById("signupName").value.trim(),email:document.getElementById("signupEmail").value.trim().toLowerCase(),password:document.getElementById("signupPassword").value});location.href="farmer.html"});login.addEventListener("submit",event=>{event.preventDefault();const user=APP.user(),email=document.getElementById("loginEmail").value.trim().toLowerCase(),password=document.getElementById("loginPassword").value,invalid=!user||user.email!==email||user.password!==password;document.getElementById("loginError").classList.toggle("hidden",!invalid);if(!invalid)location.href="farmer.html"})});
